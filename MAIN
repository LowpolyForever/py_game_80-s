#Scuffed Tetris (Brian, Leo, Harris)

import pygame
pygame.init()

#constants
SCREEN_DIMENSIONS = (640,480)

#Colors
BLACK = (0,0,0)
WHITE = (255,255,255)
BLUE = (78,113,228)
GREEN = (55,206,45)

#functions
def shape_rotate():
    pass
def draw_line():
    pass
def draw_L():
    pass
def draw_square():
    pass
def draw_pyramid():
    pass
def Randmom_shape():
    pass
def Randmom_colour():
    pass
def shape_placer():
    pass
    random_shape()
    random_colour()
    if #keypressed
        shape_rotate()
    if #keypressed
        shape_position_save()
def shape_position_save():
    pass

#Setup
screen = pygame.display.set_mode(SCREEN_DIMENSIONS)
pygame.display.set_caption ("MAIN")

game_over = False
while not game_over:
    for event in pygame.event.get():
        if event.type == pygame.KEYDOWN and event.key == pygame.K_ESCAPE:
            game_over = True

    pygame.draw.rect((screen), (WHITE), ((110,30),(420,420)), (5)) #make tetris boundary

    pygame.display.flip()

    





